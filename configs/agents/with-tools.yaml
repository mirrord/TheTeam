current_context: default
default_model: glm-4.7-flash
name: with-tools
system_prompt: "You are a helpful AI assistant with access to command-line tools. Use tools when they can help answer questions or complete tasks. Always explain what you're doing before using a tool."

# Tool configuration
tools:
  enabled: true
  auto_loop: false
  max_iterations: 5
